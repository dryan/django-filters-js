/*
 * django-filters.js is a JavaScript implementation of Django's template filters.
 * Copyright (c) 2020 Dan Ryan. Available under the MIT License.
 */
import { addslashes, addSlashes } from "./addslashes";
import { apnumber, apNumber } from "./apnumber";
import { capfirst, capFirst } from "./capfirst";
import { center } from "./center";
import { cut } from "./cut";
import { date } from "./date";
import { escape } from "./escape";
import { escapejs, escapeJs } from "./escapejs";
import { filesizeformat, fileSizeFormat } from "./filesizeformat";
import { force_escape, forceEscape } from "./force_escape";
import { floatformat, floatFormat } from "./floatformat";
import { intcomma } from "./intcomma";
import { iriencode, iriEncode } from "./iriencode";
import { linebreaks } from "./linebreaks";
import { linebreaksbr, linebreaksBr } from "./linebreaksbr";
import { linenumbers, lineNumbers } from "./linenumbers";
import { ljust } from "./ljust";
import { ordinal } from "./ordinal";
import { phone2numeric, phone2Numeric } from "./phone2numeric";
import { pluralize } from "./pluralize";
import { rjust } from "./rjust";
import { slugify } from "./slugify";
import { striptags, stripTags } from "./striptags";
import { time } from "./time";
import { timesince, timeSince, timeuntil, timeUntil } from "./timesince";
import { title } from "./title";
import { upper } from "./upper";
import { urlencode, urlEncode } from "./urlencode";
export { addslashes, addSlashes, apnumber, apNumber, capfirst, capFirst, center, cut, date, escape, escapejs, escapeJs, filesizeformat, fileSizeFormat, force_escape, forceEscape, floatformat, floatFormat, intcomma, iriencode, iriEncode, linebreaks, linebreaksbr, linebreaksBr, linenumbers, lineNumbers, ljust, ordinal, phone2numeric, phone2Numeric, pluralize, rjust, slugify, striptags, stripTags, time, timesince, timeSince, timeuntil, timeUntil, title, upper, urlencode, urlEncode, };
export default {
    addslashes,
    addSlashes,
    apnumber,
    apNumber,
    capfirst,
    capFirst,
    center,
    cut,
    date,
    escape,
    escapejs,
    escapeJs,
    filesizeformat,
    fileSizeFormat,
    force_escape,
    forceEscape,
    floatformat,
    floatFormat,
    intcomma,
    iriencode,
    iriEncode,
    linebreaks,
    linebreaksbr,
    linebreaksBr,
    linenumbers,
    lineNumbers,
    ljust,
    ordinal,
    phone2numeric,
    phone2Numeric,
    pluralize,
    rjust,
    slugify,
    striptags,
    stripTags,
    time,
    timesince,
    timeSince,
    timeuntil,
    timeUntil,
    title,
    upper,
    urlencode,
    urlEncode,
};
//# sourceMappingURL=index.js.map